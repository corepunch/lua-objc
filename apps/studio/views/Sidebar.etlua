<VStack id="sidebar" padding="<%= collapsed and metrics.collapsedPadding or metrics.expandedPadding %>" spacing="10" width="<%= collapsed and metrics.collapsedWidth or metrics.expandedWidth %>" maxHeight="infinity" alignment="leading" background="secondaryBackground">
	<% if not collapsed then %>
	<Button title="New Project" systemImage="plus" style="borderedProminent" maxWidth="infinity" />
	<Label text="RECENT PROJECTS" size="10" weight="semibold" color="secondary" />
	<% end %>
	<VStack padding="0" spacing="5" maxWidth="infinity" alignment="leading">
		<% for _, project in ipairs(projects) do %>
		<%- partial("SidebarItem.etlua", {item = project, collapsed = collapsed, metrics = metrics}) %>
		<% end %>
	</VStack>
	<Spacer />
	<Divider />
	<% for _, item in ipairs(links) do %>
	<%- partial("SidebarItem.etlua", {item = item, collapsed = collapsed, metrics = metrics}) %>
	<% end %>
	<% if not collapsed then %>
	<Label text="Documents · local workspace" size="10" color="secondary" lines="2" />
	<% end %>
</VStack>
