# Demos

This directory contains runnable framework examples and feature demos. Their
Lua modules use the `demo.<name>` namespace.

Product apps live in `apps/`; runnable test harness apps live in `test/`; and
headless regression scripts live in `tests/`.

Run a demo directly, for example:

```sh
./lua-objc demo/controls
```
